<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.layoutpicker.layoutpicker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/fairmontflyerslogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="layoutpicker.layoutpicker.layoutpicker.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="layoutpicker.layoutpicker.layoutpicker.CoursePicker"/>
        <activity android:name="layoutpicker.layoutpicker.layoutpicker.CourseInformation"/>
        <activity android:name="layoutpicker.layoutpicker.layoutpicker.UpcomingEvents"/>
        <activity android:name="layoutpicker.layoutpicker.layoutpicker.ScoreCard"/>
    </application>
</manifest>